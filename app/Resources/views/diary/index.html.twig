{% extends 'base.html.twig' %}

{% block header %}
    My Diary
{% endblock %}

{% block body %}
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>Date and time</th>
            <th>Note</th>
            <th>Category</th>
        </tr>
        </thead>
        <tbody>
        {% for diaryEntry in diaryEntries %}
            <tr onclick="window.location.href='{{ path('paginate', {'page': diaryEntry.id}) }}';">
                <td>{{ diaryEntry.datetime | date("Y-m-d h:i") }}</td>
                <td>{{ diaryEntry.getTitle() }}</td>
                <td>{{ diaryEntry.category }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ include('diary/index.paginator.html.twig') }}

{% endblock %}
